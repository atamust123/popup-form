/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export",
  // basePath: "/login-app",
  // experimental: {
  // swcPlugins: [["next-superjson-plugin", {}]],
  // },
};

module.exports = nextConfig;
